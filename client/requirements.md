## Packages
framer-motion | Page transitions and complex animations
lucide-react | Iconography
clsx | Class name utility (already in base but good to list)
tailwind-merge | Class merging (already in base but good to list)

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  sans: ["var(--font-sans)"],
  display: ["var(--font-display)"],
}
